<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Padaria Pão Saboroso — Cadastro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/app.css">
    <style>
        body { display: grid; place-items: center; min-height: 100vh; padding: 24px; }
        .cadastro-card {
            width: min(520px, 100%);
            padding: 40px;
            border-radius: 32px;
            background: rgba(255, 253, 248, 0.88);
            border: 1px solid var(--line);
            box-shadow: var(--shadow);
            backdrop-filter: blur(18px);
        }
        .cadastro-card h1 { font-family: 'Fraunces', serif; font-size: 2rem; margin: 8px 0 4px; }
        .cadastro-card > p { color: var(--muted); margin: 0 0 28px; }
        .cadastro-card label { display: grid; gap: 6px; font-weight: 700; color: var(--muted); margin-bottom: 16px; }
        .cadastro-card input, .cadastro-card select {
            width: 100%; border: 1px solid var(--line); border-radius: 14px;
            padding: 12px 14px; background: rgba(255,255,255,0.82);
            color: var(--text); font: inherit;
        }
        .cadastro-card input:focus, .cadastro-card select:focus { outline: 2px solid rgba(180,79,47,0.22); border-color: var(--accent); }
        .cadastro-card .button { width: 100%; padding: 14px; border-radius: 14px; font-size: 1rem; margin-top: 8px; }
        .back-link { display: block; text-align: center; margin-top: 16px; font-size: 0.88rem; color: var(--accent); font-weight: 700; }
    </style>
</head>
<body>
    <div class="cadastro-card">
        <p class="eyebrow">🥐 Padaria Pão Saboroso</p>
        <h1>Criar conta</h1>
        <p>Preencha os dados para acessar o sistema.</p>

        <form action="/User" method="post">
            <label for="username">
                Nome completo
                <input type="text" id="username" name="username" placeholder="Ex.: Maria Oliveira" required>
            </label>

            <label for="email">
                E-mail
                <input type="email" id="email" name="email" placeholder="seu@email.com" required>
            </label>

            <label for="perfil">
                Perfil de acesso
                <select id="perfil" name="perfil" required>
                    <option value="">Selecione o perfil</option>
                    <option value="admin">Administrador</option>
                    <option value="cliente">Atendente</option>
                </select>
            </label>

            <label for="password">
                Senha
                <input type="password" id="password" name="password" placeholder="••••••••" required>
            </label>

            <label for="confirmPassword">
                Confirmar senha
                <input type="password" id="confirmPassword" name="confirmPassword" placeholder="••••••••" required>
            </label>

            <button class="button" type="submit">Cadastrar</button>
        </form>

        <a class="back-link" href="/login">← Voltar ao login</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
